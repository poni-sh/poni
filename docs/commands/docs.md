### `poni docs`

Generate and maintain documentation.

```bash
poni docs list                  # list configured targets
poni docs generate              # regenerate all
poni docs generate <target>     # regenerate specific target
poni docs generate --changed    # only changed sources
poni docs diff                  # dry run, show changes
```
